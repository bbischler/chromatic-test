import { Meta, Story, ArgsTable } from '@storybook/addon-docs/blocks';
import Button from './button.component';
import { moduleMetadata } from '@storybook/angular';

<Meta title="MDX/Buttons" component={Button} />


# Basic Button

This is an example on how to use the `ButtonComponent`.

```
<storybook-button 
    [label]='label' 
    [size]='small' 
    (clicked)='buttonClicked()'>
</storybook-button>
```

<ArgsTable of={Button} />
